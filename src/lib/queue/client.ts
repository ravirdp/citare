// ── Redis client stub ──
// Upstash Redis instance was deleted (2026-03-27).
// This module preserves the function signature so callers don't break.
// All callers already handle null gracefully.

export function getRedis(): null {
  return null;
}
